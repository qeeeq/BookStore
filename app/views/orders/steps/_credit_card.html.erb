

<%= form_with model: @order do |card_form| %>
  <%= card_form.collection_select(:order, current_customer.credit_cards, :id, :number) %>
<% end %>

  <!-- button to call OrdersController.update -->
<%= button_to "Ok", { :action => 'update', :id => @order.id}, :method => :put %>










  <%= form_with model: @order, method: :patch do |card_form| %>
    <%= label_tag("Select card:") %>
    <%= card_form.collection_select('credit_card[credit_card_id]', current_customer.credit_cards, :id, :number, selected: @order.credit_card) %>
  <% end %>

  <%= form_with model: @order do |card_form| %>
    <%= card_form.select(:credit_card_id, [credit_card_id) %>
  <% end %>

  <%= form_with model: @person do |person_form| %>
    <%= person_form.collection_select(:city_id, City.all, :id, :name) %>
  <% end %>


<!-- button_to "Ok", { :controller => 'orders_controller', :id => @order.id}, :method => :put 
button_to "Delete Image", { action: "delete", id: @image.id }, method: :delete -->

<%# if (current_customer? find?)credit_card.exists?(customer_id: customer.id)
  # same as Business.where(user_id: current_user.id).exists?
  # ...
else
  # ...
end
%>
